# 县域资源数字银行｜种植选址 MVP 技术方案

> 当前范围：福建省永春县；面向企业的农业种植选址 Demo。  
> 本文只定义第一版可运行闭环，不要求达到政府审批或正式投资决策精度。

## 1. MVP 要验证的问题

第一版只验证一件事：

> 企业输入种植作物、所需面积和偏好后，系统能否从永春县内生成一批候选土地，完成硬性筛选、软性排序，并在地图上给出可解释的推荐结果。

MVP 不追求：

- 识别法律意义上的真实可开发地块；
- 覆盖所有产业和所有作物；
- 精确估算投资回报率；
- 替代国土空间规划、土地权属核验和政府审批；
- 训练高精度的自有遥感模型。

## 2. Demo 的最终形式

Demo 是一个在浏览器中打开的本地网页，包含两个主要页面。

### 2.1 企业需求页

企业填写：

- 种植作物；
- 所需连续土地面积，单位为亩；
- 是否需要灌溉；
- 目标市场或销售方向，可选；
- 成本、市场、风险、政策四个维度的偏好权重。

第一版只支持预先配置的少量作物，不支持任意产业自动生成指标。

### 2.2 推荐结果页

页面展示：

- 永春县交互式地图；
- 疑似撂荒种植候选地；
- 被硬性条件淘汰的区域；
- Top 3—5 推荐土地；
- 每块土地的面积、综合得分和四个一级指标得分；
- 推荐理由、不利因素、数据来源和数据更新时间；
- “结果需由政府进一步核验”的提示。

用户点击地图上的某块候选地后，可以查看该土地的指标卡片。

## 3. 候选土地的定义

第一版不把遥感结果直接称为法定“可开发土地”，统一称为：

> 疑似撂荒种植候选地

候选地采用以下代理规则生成：

```text
历史时期表现为耕地
AND 最近连续若干时期不再表现为耕地
AND 当前主要表现为草地、灌木或裸地
AND 不属于水体、建筑或稳定森林
AND 不与当前已收集的自然保护区重叠
= 疑似撂荒候选像素
```

遥感只能识别地表覆盖及其变化，不能证明土地已经弃用、可以流转或允许开发。因此结果必须保留“疑似”和“待政府核验”的表述。

## 4. 候选土地生成流程

### 4.1 数据来源

- 永春县行政区边界；
- ESA WorldCover 2020/2021，作为历史土地覆盖基准；
- Dynamic World 或 Sentinel-2，作为近年土地覆盖及变化数据；
- 可公开获得的自然保护区边界；
- 气候、土壤团队输出的可行性结果，后续接入。

### 4.2 处理过程

```text
裁剪永春县范围
→ 提取历史耕地
→ 计算近年土地覆盖类型
→ 识别“耕地转为草地/灌木/裸地”的像素
→ 排除水体、建筑、稳定森林
→ 减去已知自然保护区
→ 接入气候和土壤可行性掩膜
→ 合并空间上相邻的候选像素
→ 转换为候选土地多边形
→ 计算每块土地面积
→ 分配 land_id
```

第一版不评价地块是否狭长、破碎或形状规则，但必须合并相邻像素，避免把空间上分散的面积错误算成一块土地。

## 5. 栅格和多边形的选择

本项目不在栅格与多边形之间二选一，而是采用混合流程：

- 遥感识别和变化检测使用栅格；
- 面积筛选、指标关联、网页交互和最终展示使用多边形。

栅格适合逐像素计算；多边形更接近用户理解的“一块地”。二者都属于 GIS 数据，也都可以通过 Python 处理。

建议保存：

- 中间遥感结果：GeoTIFF；
- 最终候选土地：GeoPackage；
- 网页接口数据：从 GeoPackage 按需转换为 GeoJSON。

## 6. 两层筛选模型

### 6.1 第一层：Hard Filter

当前版本的硬性条件包括：

1. 候选地位于永春县内；
2. 符合疑似撂荒候选规则；
3. 不在当前已知自然保护区内；
4. 气候条件满足，由其他模块提供；
5. 土壤条件满足，由其他模块提供；
6. 单块连续土地面积不小于企业需求面积。

面积仅作为硬筛选条件，不参与软性评分。

对于缺失数据，应记录为 `unknown`，不应自动记为 0 分或判定不满足。

### 6.2 第二层：Soft Ranking

通过硬性筛选的土地，按照以下维度排序：

1. 成本；
2. 市场/收益；
3. 风险；
4. 政策。

第一版采用透明的加权求和：

```text
总分 = 成本分 × 成本权重
     + 市场分 × 市场权重
     + 风险分 × 风险权重
     + 政策分 × 政策权重
```

县级指标允许所有候选地取相同值。这些指标会出现在结果解释中，但不会造成县内候选地之间的排序差异。

## 7. 第一版计划采集的指标数据

气候和土壤暂不在本模块采集。当前模块重点采集：

### 7.1 成本

- 距主要道路距离；
- 距高速入口距离；
- 距乡镇中心距离；
- 农村土地流转或租赁价格，能够获得时使用；
- 县级人工成本；
- 县级农业用水、用电等成本。

### 7.2 市场/收益

- 距农产品市场距离；
- 距物流、冷链节点距离；
- 周边人口；
- 周边农业企业、合作社和加工企业数量；
- 与目标市场的交通距离。

### 7.3 风险

- 已公开的洪水、地质灾害风险区；
- 与河流的距离等风险代理指标；
- 历史自然灾害事件；
- 县级经营或治理风险信息，仅作背景说明。

### 7.4 政策

- 永春县农业扶持政策；
- 作物或产业专项政策；
- 补贴金额、比例及申请条件；
- 政策有效期；
- 适用对象和区域。

政策数据在县内相同时只用于解释；如果某项政策限定特定乡镇或园区，则可以参与候选地排序。

## 8. 推荐技术方案

### 8.1 第一版不用 Node.js

MVP 建议全部使用 Python，原因是：

- 遥感、GIS、指标计算都以 Python 生态为主；
- 不需要同时维护前后端两套工程；
- Streamlit 可以快速生成表单、指标卡片和交互式地图；
- 后续如果需要正式产品界面，再把前端替换为 React/Vue。

推荐组件：

| 模块 | 技术 |
|---|---|
| Demo 网页 | Streamlit |
| 交互地图 | Folium/Leaflet 或 PyDeck |
| 矢量空间处理 | GeoPandas、Shapely |
| 栅格处理 | Rasterio、GDAL |
| 遥感云端处理 | Google Earth Engine Python API |
| 指标计算 | Pandas、NumPy |
| 图表 | Plotly |

### 8.2 第一版数据库使用 GeoPackage

第一版暂不启动 PostgreSQL/PostGIS，使用一个 `*.gpkg` GeoPackage 文件保存候选土地及其属性。

选择 GeoPackage 的原因：

- 它本质上是支持空间数据的 SQLite 文件；
- 一个文件即可保存多边形和指标字段；
- QGIS、Python和常见GIS软件都能直接打开；
- 无需运行数据库服务器；
- 对永春一个县的数据量足够；
- 后续可以迁移到 PostgreSQL/PostGIS。

建议的数据组织：

```text
data/
  raw/                 原始数据
  interim/             中间栅格结果
  processed/
    yongchun_mvp.gpkg  候选土地和空间指标
    indicators.parquet
```

当系统需要多用户、在线更新、并发查询或者扩展到大量县域时，再切换到 PostgreSQL/PostGIS。

## 9. 候选土地数据结构

每块土地至少包含：

| 字段 | 含义 |
|---|---|
| `land_id` | 候选土地唯一编号 |
| `geometry` | 土地边界多边形 |
| `area_mu` | 面积，亩 |
| `historical_class` | 历史土地覆盖类型 |
| `current_class` | 当前土地覆盖类型 |
| `abandoned_confidence` | 疑似撂荒置信度 |
| `protected_overlap` | 是否与已知保护区重叠 |
| `climate_pass` | 气候是否满足 |
| `soil_pass` | 土壤是否满足 |
| `cost_score` | 成本得分 |
| `market_score` | 市场/收益得分 |
| `risk_score` | 风险得分 |
| `policy_score` | 政策得分 |
| `total_score` | 综合得分 |
| `data_completeness` | 数据完整度 |

## 10. 本地运行方式

系统分为两个步骤。

### 10.1 离线预处理

遥感分类、保护区扣除、栅格转多边形和空间指标计算提前执行一次，生成候选土地数据库。用户每次打开网页时不重新处理全部遥感影像。

### 10.2 在线交互

Streamlit 网页只负责：

- 接收企业输入；
- 根据面积等条件查询候选土地；
- 根据用户权重重新计算排名；
- 在地图上显示结果。

这种方式计算压力小，Demo 响应会更快。

## 11. 当前 Mac 的运行可行性

当前设备为 Apple M3 MacBook Air，8 核 CPU、16 GB 内存，约有 72 GB 可用磁盘空间，已经安装 Python、Node.js 和 Docker。

结论：可以运行本项目的永春县 MVP。

适合在本地完成：

- Streamlit 网页；
- GeoPackage 查询；
- 永春县范围的矢量空间计算；
- 已裁剪的小范围栅格处理；
- 指标计算和地图展示。

不建议在本地长期保存或批量处理全国、全省的多年份原始 Sentinel-2 影像。此类计算优先使用 Google Earth Engine，只把永春县裁剪后的最终结果下载到本地。

系统 Python 版本不直接用于本项目。项目已经建立独立的 Python 3.12 虚拟环境 `.venv`，并通过 `pyproject.toml` 和 `uv.lock` 固定依赖，不改动系统 Python。

## 12. MVP 开发顺序

### 阶段一：候选地闭环

1. 获得永春县边界；
2. 获取历史和近期土地覆盖数据；
3. 生成疑似撂荒栅格；
4. 扣除已知自然保护区；
5. 将相邻像素合并并转为候选土地多边形；
6. 计算土地面积并生成 `land_id`；
7. 在地图上显示候选土地。

### 阶段二：数据和排序

1. 获取道路、市场、物流和农业企业等数据；
2. 获取风险和政策数据；
3. 计算候选地空间指标；
4. 实现面积硬筛选；
5. 实现四维度加权排序；
6. 输出推荐理由和数据完整度。

### 阶段三：完整 Demo

1. 制作企业需求表单；
2. 制作交互地图和土地详情卡片；
3. 接入气候、土壤团队的结果；
4. 选取少量区域进行人工目视核验；
5. 准备给老师和政府演示的固定案例。

## 13. 第一版成功标准

满足以下条件即可认为 MVP 成功：

- 地图上能够看到真实位于永春县的候选土地多边形；
- 用户输入面积后能够淘汰面积不足的土地；
- 剩余土地能够按照四个维度排序；
- 用户可以点击土地查看面积、得分和推荐理由；
- 每个指标能够追溯数据来源；
- 页面明确展示遥感识别和公开数据的局限；
- 整个流程可以在当前 Mac 上重复运行。

## 14. 当前已实现版本（截至2026年9月15日）

### 14.1 已跑通的完整流程

当前代码已经形成一条能够在本地重复运行的 MVP 闭环：

```text
公开遥感土地覆盖数据
→ 识别疑似撂荒像素
→ 合并相邻像素并转为候选地多边形
→ 计算面积和空间指标
→ 企业选择作物、面积和排序权重
→ 面积等硬条件筛选
→ 成本、市场、风险、政策四维加权排序
→ 在交互地图展示候选地和推荐理由
```

最终生成84块位于永春县范围内的疑似撂荒种植候选地。默认输入“水稻、至少50亩”时，有5块土地通过面积硬筛选。

当前候选地只表示根据公开遥感土地覆盖变化识别出的技术候选区域，不表示已经完成权属、规划用途、基本农田、生态红线或政府审批核验。

### 14.2 已实现的企业输入和页面功能

Streamlit交互页面已经实现：

- 选择种植作物；
- 输入所需连续面积，单位为亩；
- 选择是否要求灌溉；
- 调整成本、市场/收益、低风险、政策支持四项权重；
- 根据面积执行硬筛选；
- 根据权重实时重新计算综合排名；
- 在地图中分别显示全部候选地和通过筛选的土地；
- 点击土地查看面积、排名、总分、一级指标分和推荐解释；
- 展示Top 5推荐土地及主要优势、相对短板；
- 显示土地租金来源层级、最低工资、水价、电价等关键原始值；
- 显示遥感结果和缺失数据的风险提示。

当前支持9类作物：水稻、玉米、大豆、甘薯、马铃薯、花生、茶、柑橘和竹类。

### 14.3 候选土地数据

候选地使用Impact Observatory/Esri年度10米土地覆盖数据生成，当前规则为：

```text
2017年为耕地类别5
AND 2022年、2023年连续变为裸地类别8或rangeland类别11
AND 候选斑块面积不小于5亩
= 疑似撂荒种植候选地
```

相邻候选像素在栅格转矢量过程中合并为多边形，使用永春县所在的UTM 50N投影计算面积，并为每块土地生成唯一的 `land_id`。候选地保存在：

- `data/processed/yongchun_mvp.gpkg`：候选土地基础数据；
- `data/processed/yongchun_ranked.gpkg`：加入空间指标和四维评分后的数据。

当前尚未取得完整、可核验的自然保护区或生态红线边界，因此 `protected_overlap` 为 `unknown`，没有把“缺少数据”错误记录成“不重叠”。气候、土壤和灌溉条件同样保留为 `unknown`，等待其他模块或政府数据接入。

### 14.4 已接入的成本指标

当前 `cost_score` 由以下指标构成：

| 指标 | 数据及实现情况 | 成本分内部权重 |
|---|---|---:|
| 距主要道路距离 | OSM福建PBF，道路矢量空间距离 | 25% |
| 距高速出入口距离 | OSM高速连接点空间距离 | 20% |
| 距乡镇或居民点距离 | OSM居民点空间距离 | 10% |
| 土地流转租金 | 永春县政府公开的24条土地流转样本 | 20% |
| 人工成本 | 永春县最低工资2045元/月 | 10% |
| 水电成本 | 非居民水价2.35元/吨、农业低压电价0.575元/千瓦时 | 15% |

土地流转样本覆盖下洋镇、桃城镇和介福乡。候选地匹配到有样本的乡镇时使用该乡镇租金中位数；其他乡镇使用24条县内样本的总体中位数231.15元/亩/年回填。

候选地所属乡镇目前按最近乡镇中心推测，并非使用乡镇行政边界确定，因此必须保留数据来源层级并等待政府复核。

最低工资、水价和电价均为县级或省级统一值。它们已进入成本模型并完成0—100归一化处理，但因为县内所有土地取值相同，不会改变永春县内部土地的相对顺序，主要用于解释和以后跨县比较。

### 14.5 已接入的市场/收益指标

当前 `market_score` 由以下指标构成：

| 指标 | 数据及实现情况 | 市场分内部权重 |
|---|---|---:|
| 周边人口 | WorldPop 2020年约100米人口栅格，汇总候选地周边5公里人口 | 45% |
| 距农产品市场距离 | OSM市场POI距离 | 20% |
| 距物流或冷链节点距离 | OSM物流POI距离 | 20% |
| 周边农业企业、合作社等POI数量 | 候选地周边10公里计数 | 15% |

目前从OSM只提取到1条市场POI、1条农业相关POI，没有提取到有效物流冷链POI。因此市场/收益分已经能够计算，但主要由周边人口和道路可达性支撑，不能解释为完整的市场规模或产业生态评价。物流缺失值当前使用中性归一化分，避免将“没有采集到”误判为“当地没有”。

### 14.6 已接入的风险指标

当前 `risk_score` 表示“风险越低，得分越高”，由以下部分组成：

- 距河流距离形成的洪水暴露代理，占风险分75%；
- 县级极端天气安全分，占风险分25%。

县级极端天气数据包括：

- NASA POWER 2021—2025年逐日降雨量、最高温和最低温；
- 每年日降雨量不低于50毫米和100毫米的天数；
- 每年最高温不低于35摄氏度的天数；
- 每年最低温不高于0摄氏度的天数；
- 永春县政府公开材料中的台风、暴雨和强对流过程次数。

该气象分是县级背景值，在县内所有候选地上相同。距河流距离只是一项快速代理，不能替代正式洪水淹没图、水文模型或地质灾害风险区。地质灾害乡镇、村级公开名单已经找到，但由于缺少稳定坐标或完整风险区边界，目前尚未进入评分。

### 14.7 已实现的政策评分

政策模块已经接入永春县粮油生产扶持措施、耕地地力保护和规模种粮补贴，以及十四五、十五五规划。政策先保留原始0—3级，再归一化到0—100参与四维加权：

| 原始等级 | 当前含义 | 归一化分 |
|---:|---|---:|
| 0 | 未找到支持，或十四五支持但十五五未继续支持 | 0 |
| 1 | 只有一般鼓励性表述 | 33.3 |
| 2 | 有明确产业政策，但未匹配当前已采集的具体补贴门槛 | 66.7 |
| 3 | 作物和土地面积匹配到公开补贴或专项支持条件 | 100 |

政策分会根据企业选择的作物和候选地面积动态计算。例如，粮食作物达到相应面积门槛后可以匹配复垦种粮、规模种粮等政策；茶、柑橘和竹类可匹配十五五产业支持方向。

当前自动规则实际已经产生0、2、3级；1级的口径已经保留，但尚未建立“只有一般鼓励性表述”的独立文本识别规则，因此当前数据中没有1级样本。

十四五—十五五连续性当前由规划原文人工结构化，没有调用额外LLM接口。代码已经实现“十四五明确推进、十五五未继续明确支持时记0分并提示当前不建议”的逻辑。当前9类作物中尚未出现实际的政策中断样本。

### 14.8 硬筛选和缺失值规则

当前硬筛选遵循以下规则：

- 面积小于企业需求的土地直接淘汰；
- 明确与保护区重叠的土地淘汰；
- 气候或土壤明确不满足的土地淘汰；
- 缺失或尚未核验的数据统一保存为 `unknown`；
- `unknown` 不自动记成0分，也不伪装成已经满足，而是在页面提示后保留为待核验候选地。

面积只参与硬筛选，不进入软排序。候选地形状、坡度、连片经营难度和权属破碎情况暂未评价。

### 14.9 归一化和总排序

距离、人口、租金等不同单位的原始指标先分别归一化到0—100。距离和成本通常按“越低越好”转换，人口、企业数量和安全表现按“越高越好”转换。

用户设置的四项一级指标权重会先自动归一化，再计算：

```text
总分 = 成本分 × 归一化成本权重
     + 市场分 × 归一化市场权重
     + 低风险分 × 归一化风险权重
     + 政策分 × 归一化政策权重
```

当四项权重全部设为0时，系统自动改用四项等权，避免除零或产生无意义结果。

### 14.10 数据目录与可追溯性

当前主要输出包括：

| 文件 | 内容 |
|---|---|
| `data/processed/yongchun_ranked.gpkg` | 84块候选地、几何边界和全部评分字段 |
| `data/processed/osm_features.gpkg` | 道路、河流、居民点、市场等OSM要素 |
| `data/processed/land_transfer_samples.csv` | 24条土地流转价格样本 |
| `data/processed/planting_subsidies.json` | 可按作物和面积匹配的补贴规则 |
| `data/processed/policy_continuity.json` | 9类作物的十四五—十五五连续性 |
| `data/processed/county_context.json` | 工资、水电价格、极端天气和灾害背景 |
| `data/processed/indicator_catalog.csv` | 指标来源、空间时间分辨率、采集方法、状态和局限 |
| `data/processed/public_data_sources.csv` | 土地流转和补贴网页来源清单 |

原始网页、NASA数据、OSM PBF和遥感栅格保存在 `data/raw` 或 `data/interim`。这两个目录体积较大，已通过 `.gitignore` 排除，不随普通Git提交保存。

### 14.11 当前验收结果

当前版本已经完成以下验证：

- 自动化测试共14项，全部通过；
- Python代码可以完成编译检查；
- 交互页面可以正常启动；
- 默认50亩面积要求下，84块候选地中有5块通过；
- 地图可缩放、切换图层并点击查看土地信息；
- 修改一级指标权重后能够重新排序；
- 页面能够显示政策0—3等级和0—100归一化结果；
- 页面能够显示土地租金回填层级、最低工资、水价和农业电价；
- 页面没有把气候、土壤、保护区等缺失数据误写为已经满足。

### 14.12 尚未完成及正式使用前必须补充的内容

当前仍未完成：

1. 完整自然保护区、生态红线和永久基本农田边界；
2. 气候、土壤团队输出的作物适宜性硬筛选结果；
3. 灌溉设施、可用水源和农业灌溉价格；
4. 乡镇行政边界与候选地的精确空间归属；
5. 完整的农产品市场、冷链、物流、企业、合作社和加工企业名录；
6. 洪水淹没区和带坐标的地质灾害风险数据；
7. 土地权属、流转意愿、规划用途和政府审批数据；
8. 对排名靠前候选地进行卫星影像和现场人工核验；
9. 人均收入、消费水平、行业企业新增注销等扩展指标；
10. 面向多县、多用户部署时使用的PostgreSQL/PostGIS数据库。

因此，当前版本适合用于验证技术路线、展示交互流程和讨论指标模型，不应直接作为企业投资或政府审批结论。

## 15. 当前代码运行方法

### 15.1 直接运行已有成果

项目已经包含处理完成的候选地与指标数据。打开终端并执行：

```bash
cd /Users/guoyantong/Documents/gover
source .venv/bin/activate
streamlit run app.py
```

终端出现地址后，在浏览器打开：

```text
http://localhost:8501
```

停止服务时，在运行Streamlit的终端按 `Control + C`。

### 15.2 虚拟环境不存在时重新安装

项目要求Python 3.12。推荐使用已经生成的锁文件安装：

```bash
cd /Users/guoyantong/Documents/gover
UV_CACHE_DIR=.uv-cache uv sync --dev
source .venv/bin/activate
streamlit run app.py
```

如果Mac上尚未安装 `uv`，需要先安装它，或者使用Python 3.12创建虚拟环境后通过项目配置安装依赖。

### 15.3 运行测试

```bash
cd /Users/guoyantong/Documents/gover
source .venv/bin/activate
pytest -q
```

预期结果为14项测试全部通过。

### 15.4 从现有原始数据重新计算指标

正常演示不需要执行本节。只有需要更新数据或验证数据处理流程时才重新运行：

```bash
cd /Users/guoyantong/Documents/gover
source .venv/bin/activate

python scripts/extract_osm_features.py \
  --pbf data/raw/fujian-latest.osm.pbf \
  --boundary data/raw/yongchun_boundary.geojson \
  --output data/processed/osm_features.gpkg

python scripts/compute_spatial_metrics.py \
  --candidates data/processed/yongchun_mvp.gpkg \
  --features data/processed/osm_features.gpkg \
  --population data/interim/worldpop_2020_yongchun.tif \
  --land-costs data/processed/land_transfer_samples.csv \
  --county-context data/processed/county_context.json \
  --output data/processed/yongchun_ranked.gpkg
```

土地流转、补贴和县级气象背景需要更新时，可以分别运行：

```bash
python scripts/fetch_public_records.py
python scripts/fetch_climate_risk.py
python scripts/build_county_context.py
```

上述三个命令会访问外部公开网站或API；只运行页面时不需要联网。
